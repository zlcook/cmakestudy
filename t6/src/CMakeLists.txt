
FIND_PACKAGE(HELLO)
IF(HELLO_FOUND)
 ADD_EXECUTABLE(t6 main.c)
 INCLUDE_DIRECTORIES(${HELLO_INCLUDE_DIR})
 TARGET_LINK_LIBRARIES( t6 ${HELLO_LIBRARY})
 
 INSTALL(TARGETS t6 RUNTIME DESTINATION bin)
ENDIF(HELLO_FOUND)
